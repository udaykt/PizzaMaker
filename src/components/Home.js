import Header from '../containers/Header/Header';
import PizzaHub from '../containers/PizzaHub/PizzaHub';

const Home = (props) => {
  return (
    <div>
      <Header />
      <PizzaHub />
    </div>
  );
};

export default Home;

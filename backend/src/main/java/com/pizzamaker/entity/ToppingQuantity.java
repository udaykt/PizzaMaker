package com.pizzamaker.entity;

// Matches the real-world tiers used by Domino's, Pizza Hut, and Papa John's
// online ordering ("Light" / "Normal" / "Extra") rather than an arbitrary
// in-house scale.
public enum ToppingQuantity {
    LIGHT, REGULAR, EXTRA
}

package com.pizzamaker.entity;

// Real chains (Domino's) only offer a binary choice here — "Normal Bake" vs
// "Well Done" — not a 3-tier scale. Matches the actual industry pattern.
public enum BakeLevel {
    NORMAL, WELL_DONE
}
